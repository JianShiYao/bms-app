/*
 * native_sim devicetree overlay
 *
 * native_sim 上没有真实的 ADC/CAN/GPIO 外设，BMS 模块在此 board 下使用桩数据，
 * 因此本 overlay 暂为空。第二步在 STM32F405 板的 overlay 中接入真实节点：
 *   - adc / 专用 AFE 芯片节点
 *   - can1（对外通信）
 *   - gpio：接触器、均衡开关
 */

/ {
	/* 占位：后续可在此定义 bms 相关的虚拟节点 */
};
