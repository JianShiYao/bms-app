# STM32 / BMS 固件 VS Code 工程模板

把本仓库里的 `.vscode/`、`.editorconfig`、`.gitignore` 放到你的工程根目录,提交到 GitHub。
其他电脑 `git clone` 后用 VS Code 打开文件夹,装好推荐插件 + 配好下面几个环境变量,
即可获得一致的构建 / 烧录 / 调试环境。

支持 **ST-Link / J-Link** 两种调试器,**CMake / west** 两种构建系统,均通过菜单选择。

---

## 1. 每台机器需要做的一次性配置

### 安装工具(自行安装,版本写进团队文档)
- arm-none-eabi 工具链(GCC)
- CMake + Ninja(用 CMake 时)
- west + Python(用 Zephyr/west 时)
- OpenOCD 或 ST-Link 工具(STM32CubeCLT,内含 `STM32_Programmer_CLI`)
- SEGGER J-Link(用 J-Link 时,内含 `JLink.exe` / `JLinkGDBServerCL`)

### 设置环境变量(PowerShell,永久写入用户环境变量)
```powershell
# arm-none-eabi-gcc 所在的 bin 目录
[Environment]::SetEnvironmentVariable("ARM_TOOLCHAIN", "C:\path\to\gcc-arm\bin", "User")

# 仅当使用 west:目标板名
[Environment]::SetEnvironmentVariable("WEST_BOARD", "your_board_name", "User")
```
> 其余工具(openocd / JLink / STM32_Programmer_CLI)建议直接加入系统 PATH。
> 若不想加 PATH,可在 **用户级**(非工作区)settings.json 里把对应路径写成绝对路径,
> 这样不会污染仓库。

---

## 2. 怎么用(选择接口)

| 操作 | 入口 | 行为 |
|------|------|------|
| 构建 | `Ctrl+Shift+B` 或命令面板 → Run Task → **Build (选择构建系统)** | 弹菜单选 cmake/west + Debug/Release |
| 烧录 | Run Task → **Flash (选择调试器)** | 弹菜单选 stlink/jlink |
| 清理 | Run Task → **Clean** | 删除 build/ |
| 调试 | 调试面板下拉框 → **Debug (ST-Link / OpenOCD)** 或 **Debug (J-Link)** → F5 | 启动时再选要调试的 ELF(cmake/west 产物) |

直达任务 **Build: CMake** / **Build: west** 不弹菜单,适合绑快捷键或固定流程。

---

## 3. 需要按你的工程改的地方

- `c_cpp_properties.json`:`defines` 与 `includePath` 改成你的 HAL 路径和宏。
- `launch.json` / `flash.ps1`:芯片型号默认 `STM32F405RG`,换芯片时改这里。
- `build.ps1`:CMake 产物默认 `build/firmware.elf`;若你的可执行名不同,
  同步改 `flash.ps1` 与 `launch.json` 的 elf 路径选项。
- 外设寄存器视图(可选):把 `STM32F405.svd` 放到 `.vscode/svd/` 下。
  该文件较大且属厂商资源,默认在 `.gitignore` 中忽略,各自下载放置即可。

---

## 4. 提交清单
提交:`.vscode/settings.json` `extensions.json` `tasks.json` `launch.json`
`c_cpp_properties.json` `.vscode/scripts/*.ps1` `.editorconfig` `.gitignore`

不提交:任何含绝对路径/密钥的 `*.local.json`、SVD 文件、build 产物(已在 .gitignore)。
