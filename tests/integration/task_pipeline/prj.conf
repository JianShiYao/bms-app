CONFIG_ZTEST=y
CONFIG_ZBUS=y
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y

# 注：app/Kconfig 不参与隔离测试构建，CONFIG_BMS_AFE / CONFIG_BMS_PROTECTION
# 这类 app 私有 Kconfig 符号无法在此处生效（会被 Kconfig 判为未知符号）。
# 被测编排所需的 CONFIG_BMS_AFE / CONFIG_BMS_PROTECTION 由 CMakeLists.txt 的
# zephyr_compile_definitions 以等价编译宏注入（见该文件注释）。这里只放
# 真正参与测试 Kconfig 树的核心开关。
